spring:
  application:
    name: user-service
  data:
    mongodb:
      database: user-service
      host: localhost
      port: 27017
      password: bdung
      username: mongoadmin
  mail:
    host: smtp.mail.ru
    port: 465
    username: robot@goeasy.space
    password: CaMye4wcmaWWVpK7Dt0w
    properties:
      "mail.smtp.ssl.enable": true
      "mail.auth": true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.5

server:
  port:
    8090