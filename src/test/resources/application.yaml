de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.5
spring:
  mail:
    password: test
    username: test
    host: 127.0.0.1
    port: 3025 # default protocol port + 3000 as offset
    protocol: smtp
